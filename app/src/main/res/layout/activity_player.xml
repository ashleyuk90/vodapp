<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.media3.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:use_controller="true"
        app:show_buffering="when_playing"
        app:resize_mode="fit"/>

    <!-- Skip Intro Button (bottom-start) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSkipIntro"
        style="@style/VOD.Button.Secondary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="32dp"
        android:layout_marginBottom="48dp"
        android:text="@string/skip_intro"
        android:contentDescription="@string/a11y_skip_intro"
        android:visibility="gone"
        app:icon="@drawable/ic_play_arrow"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusRight="@id/btnSkipCredits"
        android:nextFocusUp="@id/player_view"/>

    <!-- Skip Credits Button (bottom-start, same position as skip intro) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSkipCredits"
        style="@style/VOD.Button.Secondary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="32dp"
        android:layout_marginBottom="48dp"
        android:text="@string/skip_credits"
        android:contentDescription="@string/a11y_skip_credits"
        android:visibility="gone"
        app:icon="@drawable/ic_play_arrow"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusRight="@id/btnNextEpisode"
        android:nextFocusUp="@id/player_view"/>

    <!-- Next Episode Button (bottom-end) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnNextEpisode"
        style="@style/VOD.Button.Primary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="100dp"
        android:text="@string/next_episode"
        android:contentDescription="@string/next_episode"
        android:visibility="gone"
        app:icon="@drawable/ic_play_arrow"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusLeft="@id/btnSkipCredits"/>

</FrameLayout>
